# Base44 development defaults — placeholders so the app boots without
# external credentials. Real values are delivered via /run/base44/app.env
# (listed AFTER this file in env_file, so they always win).
AIS_FEED_HOST=
AIS_FEED_PORT=0
AIS_FEED_PROTOCOL=tcp
AISSTREAM_API_KEY=
